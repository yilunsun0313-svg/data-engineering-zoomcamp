version: 2

models:
  - name: stg_data_test
    columns:
      - name: service_zone
        tests:
          - accepted_values:
              arguments:
                values: ['Boro Zone', 'Yellow Zone', 'EWR']
                quote: True